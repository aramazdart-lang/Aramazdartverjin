
const prices={A5:{10:{normal:3000,watercolor:3300},20:{normal:4000,watercolor:4500}},A4:{10:{normal:4000,watercolor:4500},20:{normal:5500,watercolor:6300}}};let size='A5', count=10, paper='normal', uploaded=0,currentTotal=3000;function fmt(n){return Number(n||0).toLocaleString('hy-AM')+' դր․'}function refresh(){document.querySelectorAll('.card').forEach(c=>c.classList.remove('active'));document.querySelectorAll('.card input:checked').forEach(i=>i.closest('.card')?.classList.add('active'))}function setSize(v){size=v;refresh();updateTotal()}function setCount(v){count=v;refresh();updateTotal()}function setPaper(v){paper=v;refresh();updateTotal()}function updateTotal(){currentTotal=prices[size][count][paper];document.getElementById('sumSize').textContent=size;document.getElementById('sumCount').textContent=count+' նկար';document.getElementById('sumPaper').textContent=paper==='normal'?'Սովորական':'Ջրաներկի / գուաշի';document.getElementById('total').textContent=fmt(currentTotal);document.getElementById('limitText').textContent='Պետք է կցել նվազագույնը '+count+' նկար'}function handlePhotos(e){const files=[...e.target.files];uploaded=files.length;document.getElementById('photoStatus').textContent='Կցված է '+uploaded+' նկար';const t=document.getElementById('thumbs');t.innerHTML='';files.slice(0,12).forEach(f=>{const img=document.createElement('img');img.src=URL.createObjectURL(f);t.appendChild(img)})}async function submitOrder(){if(uploaded<count){alert('Խնդրում ենք կցել նվազագույնը '+count+' նկար։');return}const childName=document.querySelector('input[placeholder="օր․ Էվա"]')?.value||'';const notes=document.querySelector('textarea')?.value||'';await AramazdCheckout.start({product:'Ներկիր ինքդ քեզ',customer_name:childName||'',price:currentTotal,deposit:currentTotal,details:{product_type:'color-yourself',size,count,paper:paper==='normal'?'Սովորական':'Ջրաներկի / գուաշի',uploaded_count:uploaded,child_name:childName,notes}})}refresh();updateTotal();
